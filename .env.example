# =======================================================================
# WEATHER SYNTH - Environment Variables Configuration
# =======================================================================

# LaunchDarkly Configuration
# ---------------------------
# Client SDK key for frontend feature flags
REACT_APP_LAUNCHDARKLY_CLIENT_ID=your-launchdarkly-client-sdk-key

# Server SDK key for backend weather provider configuration
LAUNCHDARKLY_SDK_KEY=your-launchdarkly-server-sdk-key

# Weather API Providers (All Optional)
# ------------------------------------
# The app supports 5 weather providers with automatic fallback

# 1. OpenWeatherMap (Default) - 1,000 calls/day free
OPENWEATHER_API_KEY=your-openweathermap-api-key

# 2. Tomorrow.io - 1,000 calls/day free
TOMORROW_API_KEY=your-tomorrow-io-api-key

# 3. WeatherAPI - 1 million calls/month free
WEATHERAPI_KEY=your-weatherapi-com-api-key

# 4. Visual Crossing - 1,000 calls/day free
VISUAL_CROSSING_API_KEY=your-visual-crossing-api-key

# 5. Open-Meteo - Free and unlimited (no API key needed)
# Note: Open-Meteo is always available as fallback

# Weather Provider Configuration
# ------------------------------
# Configure provider priority via LaunchDarkly flag or environment
# Available providers: openweathermap, tomorrow-io, weatherapi, visual-crossing, open-meteo
WEATHER_PROVIDER_CONFIG={"primary": "openweathermap", "fallback": "open-meteo"}

# Application Configuration
# -------------------------
REACT_APP_VERSION=1.2.0
PORT=3002

# Google Analytics (Optional)
# ---------------------------
# For usage tracking (completely optional)
REACT_APP_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# LaunchDarkly Advanced Configuration (Optional)
# ----------------------------------------------
# For proxy or custom endpoint setups
REACT_APP_LD_BASE_URL=
REACT_APP_LD_STREAM_URL=
REACT_APP_LD_EVENTS_URL=

# =======================================================================
# SETUP INSTRUCTIONS:
# 
# 1. Copy this file to .env: cp .env.example .env
# 2. Add your API keys for desired weather providers
# 3. The app works without any API keys using Open-Meteo
# 4. Configure provider priority via LaunchDarkly flags or WEATHER_PROVIDER_CONFIG
# 5. All variables are optional - app works with demo data
# =======================================================================
